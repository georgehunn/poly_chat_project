# Xcode
*.xcuserstate
xcuserdata/
DerivedData/
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but just in case)
Pods/
Podfile.lock

# Carthage (not used, but just in case)
Carthage/Build/

# Archives / distribution
*.ipa
*.dSYM.zip
*.dSYM

# Claude Code local config (personal file paths, local settings)
.claude/

# Secrets / environment files
.env
*.env.local

# Stale root-level source duplicates (real source is in poly_chat/)
/Models/
/Services/
/Resources/
